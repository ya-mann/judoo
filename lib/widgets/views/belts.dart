import 'package:judoo/customIcons.dart';
import 'package:flutter/material.dart';

class BeltsPage extends StatelessWidget {
  const BeltsPage({Key key}) : super(key: key);
  static const name = 'Ceintures';
  static const icon = Icon(CustomIcons.belt, size: 25);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}